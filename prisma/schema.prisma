datasource db {
  provider = "postgresql" // Replace with your database provider if different
  url      = env("DATABASE_URL") // Ensure this environment variable is set in your .env file
}

model PasswordReset {
  id      String   @id @default(uuid())
  email   String
  token   String
  expiry  DateTime

  @@map("password_resets")
}